@model List<LanguageModel>

@{
    ViewData["Title"] = "Languages List";
}

<div class="container">

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0 ">Language List</h2>
        <a asp-action="AddEdit" asp-controller="Language" class="btn btn-success ">Add New Language</a>
    </div>

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show mt-5" role="alert">
            @TempData["Message"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>Language Name</th>
                <th>User Name</th>
                <th>Created At</th>
                <th>Modified At</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Count > 0)
            {
                foreach (var language in Model)
                {
                    <tr>
                        <td>@language.LanguageName</td>
                        <td>@language.FullName</td>
                        <td>@(language.CreatedAt?.ToString("yyyy-MM-dd") ?? "-")</td>
                        <td>@(language.ModifiedAt?.ToString("yyyy-MM-dd") ?? "-")</td>
                        <td>
                            <a asp-action="AddEdit" asp-route-id="@language.LanguageId" class="btn btn-sm btn-primary">
                                Edit
                            </a>
                            <a asp-action="Delete" asp-route-id="@language.LanguageId"
                               onclick="return confirm('Are you sure you want to delete this language?');"
                               class="btn btn-sm btn-danger">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="5" class="text-center text-muted">No languages found.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
